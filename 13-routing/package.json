{
  "name": "13-routing",
  "version": "0.0.0",
  "scripts": {
    "dev": "clear && vite --port 8000",
    "build": "clear && vite build",
    "serve": "clear && npm run build && vite preview --port 8000"
  },
  "dependencies": {
    "vue": "^3.0.5",
    "vue-router": "^4.0.3"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^1.1.4",
    "@vue/compiler-sfc": "^3.0.5",
    "vite": "^2.0.0-beta.65"
  }
}
